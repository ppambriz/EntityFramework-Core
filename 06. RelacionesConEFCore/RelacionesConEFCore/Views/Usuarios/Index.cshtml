@model IEnumerable<RelacionesConEFCore.Models.Usuario>

@{
    ViewData["Title"] = "Usuarios";
}

<h1 class="mb-4">Usuarios</h1>

<p>
    <a asp-action="Crear" class="btn btn-primary">Nuevo Usuario</a>
</p>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Id)</th>
            <th>@Html.DisplayNameFor(model => model.Nombre)</th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th>Perfil</th>
            <th>Comentarios</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var usuario in Model)
        {
            <tr>
                <td>@usuario.Id</td>
                <td>@usuario.Nombre</td>
                <td>@usuario.Email</td>
                <td>
                    @if (usuario.Perfil != null)
                    {
                        <span>@usuario.Perfil.Biografia</span>
                    }
                    else
                    {
                        <span class="text-muted">Sin perfil</span>
                    }
                </td>
                <td>
                    @(usuario.Comentarios?.Count ?? 0)
                </td>
                <td>
                    <a asp-action="Editar" asp-route-id="@usuario.Id" class="btn btn-sm btn-warning">Editar</a>
                    <a asp-action="Detalle" asp-route-id="@usuario.Id" class="btn btn-sm btn-info">Detalles</a>
                    <a asp-action="Borrar" asp-route-id="@usuario.Id" class="btn btn-sm btn-danger">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>